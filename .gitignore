# `.claude/` holds the SPDD commands and the mirror of .codex/agents, both
# shared with the team, so re-include it here — a global gitignore commonly
# excludes `.claude`.
!.claude/
.claude/settings.local.json

# Cargo build artifacts
/target
