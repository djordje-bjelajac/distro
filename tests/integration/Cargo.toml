[package]
name = "integration-tests"
version.workspace = true
edition.workspace = true
publish.workspace = true

# Dev-dependencies only, and deliberately so: this crate ships no library. Its
# whole content is the `tests/` directory, where the canvas's acceptance
# criteria are proved end to end over `infra-sim-net` (OP-9).
[dev-dependencies]
identity = { workspace = true }
infra-sim-net = { workspace = true }
membership = { workspace = true }
messaging = { workspace = true }
shared-types = { workspace = true }

[lints]
workspace = true
